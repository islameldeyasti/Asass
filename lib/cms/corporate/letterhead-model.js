/**
 * Letterhead model — client-safe re-exports + helpers.
 */
export {
  LETTERHEAD_TEMPLATES,
  LETTERHEAD_LAYOUT,
  TEMPLATE_TOKENS,
  OFFICIAL_LETTERHEAD_PAGE_BG,
  DEFAULT_LETTERHEAD_CHROME_LAYOUT,
  normalizeLetterheadChromeLayout,
  letterheadChromeCssVars,
  createDesignLayer,
  normalizeDesignLayers,
  getTemplateTokens,
  getBodyHeightMm,
  emptyLetterhead,
  resolveLetterLanguage,
} from './letterhead-templates';
